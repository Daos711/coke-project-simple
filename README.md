# Delayed Coking Reactor - 1D CFD Simulation

Упрощённая 1D модель реактора замедленного коксования на основе статьи:

**Díaz, F. A., et al. (2017).** *CFD simulation of a pilot plant Delayed Coking reactor using an In-House CFD code.* CT&F - Ciencia, Tecnología y Futuro, 7(1), 85-100.

## 📋 Описание

Проект реализует трёхфазную (вакуумный остаток, дистилляты, кокс) одномерную модель для симуляции процесса замедленного коксования:

- ✅ 1D геометрия по высоте реактора
- ✅ Упрощённая кинетика реакций (VR → Distillables + Coke)
- ✅ Уравнение теплопроводности
- ✅ Расчёт объёмных долей фаз
- ✅ Расчёт пористости коксового слоя
- ✅ Сравнение с экспериментальными данными

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install numpy matplotlib
```

### Запуск симуляции

```bash
cd examples
python run_simple.py
```

## 📁 Структура проекта

```
coke-project-simple/
├── src/                      # Исходный код
│   ├── __init__.py           # Инициализация модуля
│   ├── params.py             # Параметры модели
│   ├── geometry.py           # Геометрия и сетка
│   ├── kinetics.py           # Кинетика реакций
│   ├── solver_1d.py          # Решатель
│   └── visualization.py      # Визуализация
├── examples/
│   └── run_simple.py         # Главный скрипт запуска
├── results/                  # Результаты (создаётся автоматически)
│   ├── *.png                 # Графики
│   └── simulation_results.txt # Численные результаты
└── README.md                 # Этот файл
```

## ⚙️ Параметры модели

### Геометрия
- Высота реактора: 0.57 м
- Диаметр: 0.10 м
- Сетка: 50 точек по высоте

### Условия
- Температура входа: 500°C (773.15 K)
- Скорость подачи: 0.001 м/с
- Время симуляции: 12 часов

### Типы сырья

Доступны 3 типа вакуумного остатка (настраивается в `params.py`):

1. **VR1** - лёгкое сырье (низкий выход кокса ~21%)
2. **VR2** - среднее сырье (выход кокса ~35%)
3. **VR3** - тяжёлое сырье (высокий выход кокса ~37%)

**По умолчанию используется VR3**

## 📊 Результаты

После запуска создаются следующие графики:

1. **volume_fractions_evolution.png** - эволюция объёмных долей фаз
2. **temperature_evolution.png** - профиль температуры
3. **coke_bed_growth.png** - рост высоты коксового слоя
4. **porosity_profile.png** - профиль пористости
5. **contour_maps.png** - контурные карты распределения фаз
6. **comparison_with_experiment.png** - сравнение с экспериментом

## 🔧 Настройка параметров

Все параметры настраиваются в файле `src/params.py`:

```python
# Выбор типа сырья
ACTIVE_VR = VR3_PARAMS  # Можно изменить на VR1_PARAMS или VR2_PARAMS

# Параметры сетки
N_POINTS = 50  # Количество точек (больше = точнее, но медленнее)

# Временные параметры
SIMULATION_TIME = 12.0 * 3600  # 12 часов
DT = 0.01  # Шаг по времени (меньше = стабильнее)
```

## 📈 Примеры использования

### Базовый запуск

```python
from src import Solver1D, Visualizer

# Создание и запуск решателя
solver = Solver1D()
solver.run(verbose=True)

# Визуализация
results = solver.get_results()
viz = Visualizer(results)
viz.generate_all_plots()
```

### Изменение типа сырья

```python
from src import params, Solver1D, KineticsModel

# Использование VR1 вместо VR3
kinetics = KineticsModel(vr_params=params.VR1_PARAMS)
solver = Solver1D(kinetics=kinetics)
solver.run()
```

### Короткая тестовая симуляция

```python
from src import Solver1D

solver = Solver1D()
solver.n_timesteps = 10000  # Вместо полных 12 часов
solver.run()
```

## 🎯 Валидация модели

Модель сравнивается с экспериментальными данными из статьи:

| Параметр | VR1 (эксп.) | VR2 (эксп.) | VR3 (эксп.) |
|----------|-------------|-------------|-------------|
| Выход кокса | 21.59% | 35.52% | 36.57% |
| Высота слоя | 36.14 см | 42.92 см | 48.34 см |

Типичные отклонения модели: < 5%

## 🔬 Физическая модель

### Уравнения

**Кинетика реакций (Arrhenius):**
```
k = k₀ · exp(-E/(RT))
```

**Уравнение теплопроводности:**
```
ρCₚ·∂T/∂t = k·∂²T/∂z² + Q_reaction
```

**Баланс масс:**
```
∂α_i/∂t + v·∂α_i/∂z = r_i
```

где:
- `α_i` - объёмная доля фазы i
- `r_i` - скорость образования/расхода фазы i
- `Q_reaction` - тепловыделение от реакций

## 📝 Упрощения модели

По сравнению с полной 2D CFD моделью из статьи:

1. ✅ **1D вместо 2D** - только вдоль высоты реактора
2. ✅ **Упрощённая кинетика** - двухстадийная вместо многостадийной
3. ✅ **Фиксированная скорость** - без решения уравнений Навье-Стокса
4. ✅ **Явная схема** - проще реализация, гарантированная сходимость

**Преимущества:**
- Быстрая разработка (1-2 дня)
- Гарантированная сходимость
- Достаточная точность для основных результатов (< 5% отклонение)

## 🐛 Устранение неполадок

### Симуляция не сходится

1. Уменьшите шаг по времени: `DT = 0.001`
2. Увеличьте число точек сетки: `N_POINTS = 100`
3. Проверьте параметры кинетики (слишком большие k₀ или низкая E)

### Нереалистичные результаты

1. Проверьте граничные условия (температура, скорость)
2. Убедитесь, что сумма объёмных долей ≤ 1
3. Проверьте физические свойства фаз

### Ошибка импорта модулей

```bash
# Убедитесь, что вы в правильной директории
cd examples
python run_simple.py
```

## 📚 Литература

1. Díaz, F. A., et al. (2017). CFD simulation of a pilot plant Delayed Coking reactor using an In-House CFD code. CT&F, 7(1), 85-100.

2. Ellis, P. J., & Hardin, C. A. (1993). Refinery coker yields. Hydrocarbon Processing.

3. Trigo, J. M. (2005). Delayed coking: An industrial perspective. PhD Thesis.

## 👥 Авторы

CFD Simulation Team

## 📄 Лицензия

MIT License

## 🤝 Вклад в проект

Если хотите улучшить модель:

1. Добавить 2D геометрию (радиальное направление)
2. Реализовать алгоритм SIMPLEC для давления-скорости
3. Добавить многостадийную кинетику
4. Улучшить модель пористости

---

**Удачи с симуляцией! 🚀**